const ActivitySelector = () => {
    return(
        <div>
            <select name="select">
                <option value="">Actividad</option>
            </select>
        </div>
    )
}

export default ActivitySelector

